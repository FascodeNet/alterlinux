language: shell
addons:
  apt:
    packages:
      - ninja
script:
  - bash -c 'shopt -s globstar nullglob; shellcheck -s bash --exclude=SC2068 -S error **/*.{sh,ksh,bash}'
  - bash -c 'make mkalteriso'
git:
  submodules: false
  language: bash