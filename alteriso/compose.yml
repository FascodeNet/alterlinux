services:
  alteriso:
    build:
      context: .
      dockerfile: Dockerfile
    privileged: true
    working_dir: /app
    volumes:
      - ./out:/app/out
      # - ./work:/app/work
    command: alteriso profile build /usr/share/alteriso/configs/xfce

